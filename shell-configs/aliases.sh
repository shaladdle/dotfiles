# tmux
alias tn='tmux new-s -s $(basename $(pwd))'
alias ta='tmux atta -t'
alias tl='tmux list-s'
alias vim='mvim -v'

# git
alias gs='git status'
alias gl='git lg'

# rust
alias cb='cargo build'

# vim
alias v='vim'
